import React from "react"

const Button = () => {
  return (
    <div>
      <button className="bg-[red] text-white text-lg border border-black">Click Me</button>
    </div>
  )
};

export default Button;
